{% extends 'base.html.twig' %}

{% block title %}Edit Children
{% endblock %}

{% block body %}
	<h1>Modifier les informations</h1>

<br>{{ include('children/_form.html.twig') }}
	<div class="d-flex align-items-center ">
		{{ include('children/_delete_form.html.twig') }}
        <a class="btn btn-primary " href="{{ path('app_children_index') }}">Retour</a>
	</div>
{% endblock %}
